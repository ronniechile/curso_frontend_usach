const fibonacci = n =>{
const fib = [0,1]

for (let i=2 ; i <= n ; i++) {
  fib[i]=fib[i-2] + fib [i-1]
  
}
return fib [n]
}

for (var n=0 ; n < 17 ;n ++){
 if (fibonacci(n)%2==0)console.log(fibonacci(n))

}
